<Window x:Class="NMTimeTracker.View.NewModifierWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NMTimeTracker.View"
        mc:Ignorable="d"
        Title="New Modifier" Height="227" Width="453">
    <Grid Margin="10">
        <Grid.Resources>
            <Style TargetType="{x:Type Label}">
                <Setter Property="Margin" Value="0,3,5,5" />
            </Style>
            <Style TargetType="{x:Type Button}">
                <Setter Property="Margin" Value="5,0,0,0" />
                <Setter Property="Padding" Value="5,0,5,0" />
                <Setter Property="MinWidth" Value="70" />
                <Setter Property="Height" Value="25" />
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Label Content="Time" HorizontalAlignment="Right" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" >
            <Button x:Name="OkButton" Content="Add modifier" Click="OkButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Click="CancelButton_Click"/>
        </StackPanel>
        <local:TimeSpanControl Grid.Column="1" VerticalAlignment="Center" Value="{Binding Time, Mode=TwoWay, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:NewModifierWindow}}}"/>
        <Label Grid.ColumnSpan="2" Content="{Binding Description, FallbackValue=[Description], Mode=OneWay, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:NewModifierWindow}}}" Grid.Row="2" VerticalContentAlignment="Bottom" HorizontalContentAlignment="Right"/>
        <Label Content="Comment" HorizontalAlignment="Right" Grid.Row="1" VerticalAlignment="Top"/>
        <TextBox x:Name="CommentTextBox" Grid.Column="1" Grid.Row="1" TextWrapping="Wrap" Text="{Binding Comment, FallbackValue=Comment..., Mode=TwoWay, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:NewModifierWindow}}}"/>
    </Grid>
</Window>
